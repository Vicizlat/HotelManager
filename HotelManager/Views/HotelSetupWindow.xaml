<Window x:Class="HotelManager.Views.HotelSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HotelManager.Views"
        mc:Ignorable="d"
        Title="Hotel Setup" Width="500" Height="300" ResizeMode="CanMinimize" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <Grid x:Name="Buildings" />
        </ScrollViewer>
        <Button Grid.Row="1" Padding="4, 2" FontSize="18" Content="Добави сграда"
                HorizontalAlignment="Left" Click="AddBuildingButtonOnClick"/>
    </Grid>
</Window>